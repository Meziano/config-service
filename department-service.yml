server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://departmentdb:5432/departmentdb    
    username: admin
    password: admin

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    de.meziane.ms.controller.DepartmentController: DEBUG
