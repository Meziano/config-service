server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://employeedb:5433/employeedb
    username: admin
    password: admin
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialec
#  sql:
#    init:
#      mode: always

logging:
  level:
    de.meziane.ms.controller.EmployeeController: DEBUG
